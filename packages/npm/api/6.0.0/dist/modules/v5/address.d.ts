import { type AddressResult } from '../v4/address.js';
/**
 * Generates one or more fictional postal addresses.
 */
export default function address(countryCode?: string, count?: number): AddressResult;
